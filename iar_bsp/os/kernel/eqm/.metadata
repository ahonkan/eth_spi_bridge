component("eqm") { 
    parent "nu.os.kern"
    description "This component implements the Nucleus Event Queue Manager."
    enable true

    library("nucleus.lib") {
        sources {
            Dir.glob("src/*.c")
        }
    }
}
